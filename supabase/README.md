# Supabase

Place Supabase-related items here:

- `migrations/` — SQL migrations for schema changes
- `policies/` — RLS policies stored as SQL or JSON
- `functions/` — Edge / serverless functions (e.g., for webhooks, background jobs)

Add setup instructions and secrets management notes as needed.
